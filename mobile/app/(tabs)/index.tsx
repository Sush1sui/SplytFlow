/* 
 * This file intentionally renders nothing.
 * The (tabs)/_layout.tsx renders HomeScreen as the default kept-alive tab,
 * so no redirect or content is needed here.
 */
export default function TabsIndex() {
  return null;
}
